# Claude Code Guide — SDK-017

```text
Implement SDK-017 as a production reporting platform.

Define report definition, dataset, parameter, job, output, schedule and delivery contracts. Implement authorization-aware data access, synchronous/asynchronous generation, safe rendering, pagination, localization, caching, cancellation, quotas, secure temporary storage, retention, scheduled delivery through SDK-015 and complete lineage.

Add production rendering adapters, large-report/load/soak tests, injection and authorization tests, metrics/alerts, customer authoring and deployment guides, retention, backup/recovery and incident runbooks. Do not execute arbitrary report code or bypass owning SDK data contracts.
```
