# Claude Code Guide — SDK-009

```text
Validate and production-harden SDK-009.

Inspect repositories, specifications, Unit of Work, transactions, migrations, provider adapters, outbox integration, security, and consumers. Produce a gap report.

Add missing optimistic concurrency, cancellation/timeouts, idempotent writes, connection resiliency, schema versioning, production relational adapter conformance, migration/rollback tooling, least-privilege roles, encryption guidance, backup/restore tests, reconciliation, load tests, metrics, customer deployment guide, and database runbook.

Do not permit cross-SDK table access or provider ORM types in public contracts. Prove upgrade and recovery in a clean customer environment.
```
