# Claude Code Guide — SDK-029

```text
Implement SDK-029 as a production API gateway framework.

Define route, upstream, match, transform, auth, quota, timeout, retry, circuit, canary and deployment contracts. Implement secure routing, identity/correlation propagation, rate limiting, size/time limits, health-aware routing, configuration validation, atomic publish/rollback, TLS/mTLS hooks, redaction and observability.

Add production runtime adapter, authorization/header/CORS/request-smuggling oriented tests, load/soak/failover tests, certificate rotation, HA/WAF guidance, dashboards/alerts, customer route deployment and incident runbooks. Do not allow arbitrary unsafe transformation code.
```
