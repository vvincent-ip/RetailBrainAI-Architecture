# Claude Code Guide — SDK-015

```text
Implement a production multichannel notification framework.

Create stable request/result/template/channel/provider contracts. Implement routing, idempotency, suppression/preferences, localization, safe rendering, rate/quota controls, bounded retry, fallback, DLQ, authenticated callbacks, delivery tracking, security/redaction and operational APIs.

Add production adapters behind ports, provider conformance, duplicate-send and outage tests, webhook-security tests, load/soak tests, metrics/alerts, customer channel onboarding, deployment/configuration, data-retention and incident runbooks.
```
